import React from 'react'
import { StyleSheet, Button } from 'react-native'

const NewCollectionModal = () => {
  return (
    <Button title="New Collection Modal" />
  )
}

export default NewCollectionModal

const styles = StyleSheet.create({})
